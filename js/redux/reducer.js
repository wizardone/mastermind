const initialState = {
  totalTurns: 8,
  currentTurn: 0,
  currentTurnCombination: null,
  selectedPeg: null,
  winningCombination: null
}

let mastermind = (state = initialState, action) => {
  return state
}

module.exports = mastermind
